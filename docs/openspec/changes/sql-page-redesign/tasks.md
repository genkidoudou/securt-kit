## 1. SQL 工作区结构

- [x] 1.1 重构 `support/http/resources/index.html` 的 SQL 区域，增加「查询 / 修改」一级切换和修改区「SQL 解析 / 参数加密」二级切换，同时保留现有控件与结果节点 ID；通过逐项核对 `app.js` 中 SQL 相关 `getElementById` 选择器验证无节点缺失
- [x] 1.2 将查询数据源、每页大小和页码整理为紧凑工具栏，并将解析与参数加密分别组织成输入/结果双栏容器；通过浏览器检查三种模式的输入、按钮和结果区域均可见且层级符合静态原型

## 2. 模式状态与事件

- [x] 2.1 重构 `app.js` 的 `sqlModes`，分别控制一级视图和修改二级模式，默认进入查询且修改默认进入 SQL 解析；通过点击全部切换组合验证每次仅显示目标面板
- [x] 2.2 移除模式切换时跨输入框复制 SQL 的逻辑，依靠持久 DOM 保留三种模式各自草稿和结果；通过在三个输入框填写不同内容并往返切换验证内容互不覆盖
- [x] 2.3 检查查询、解析、参数加密、清空和复制事件绑定在新结构下继续工作；通过浏览器执行每项操作并确认清空只影响当前模式、复制内容来自当前加密结果

## 3. 紧凑布局与响应式样式

- [x] 3.1 在 `style.css` 中增加 SQL 工具栏、一级/二级切换、双栏工作区及受限结果容器样式；通过长查询结果、长解析映射和长加密 SQL 验证内容在结果区内部滚动且整页高度不持续增长
- [x] 3.2 为查询结果表增加滚动区域内的粘性表头，并处理现有双视图渲染结构；通过生成超过可见高度的明文/密文结果验证滚动时表头可见
- [x] 3.3 增加 900px 响应式断点及 `min-width`/`min-height` 防溢出规则；通过大于 900px 时双栏、小于等于 900px 时上下布局的浏览器检查验证无页面级横向滚动

## 4. 回归验证

- [x] 4.1 在测试应用中对 SQL 查询、SQL 解析和参数加密各执行一次成功流程及一次输入错误流程，验证仍调用现有接口并正确展示成功或错误反馈
- [x] 4.2 验证数据源选择、查询分页、执行 SQL、明文/密文双视图、解析映射、加密字段、处理统计和复制按钮均保留且可用
- [x] 4.3 运行 `mvn -pl securt-kit-monitor -am test`，验证 Monitor 及依赖模块测试通过且无后端 API 回归
- [x] 4.4 更新 `docs/superpowers/specs/2026-09-08-sql-page-redesign-design.md` 状态为已实现，并核对最终页面与 `docs/superpowers/mockups/sql-page-redesign/index.html` 的已确认结构一致
