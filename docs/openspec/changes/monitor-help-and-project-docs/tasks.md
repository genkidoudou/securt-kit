## 1. 回归测试基线

- [x] 1.1 扩展 `SqlWorkspaceResourceTest` 或新增 Monitor 文档资源测试，断言六个 `data-help-topic` 入口、六个帮助模板、共用 `role="dialog"` 弹窗和 `data-tab="docs"` 存在；运行指定测试并确认当前实现按预期失败
- [x] 1.2 增加项目文档主题、代码复制目标、帮助关闭路径、焦点恢复、内部滚动及 900px 响应式规则的资源断言；运行指定测试并确认缺失行为均被测试捕获

## 2. 页面结构与内容

- [x] 2.1 重构 `support/http/resources/index.html` 的六个主功能标题区，加入带眼睛 SVG、可见「使用说明」文本、`aria-label` 和正确 `data-help-topic` 的按钮；逐项核对现有表单与操作控件 ID 未变化
- [x] 2.2 在 `index.html` 增加单个共用帮助弹窗和六个静态帮助模板，确保每个模板包含用途、步骤、字段或结果解释、限制和最小示例；通过资源测试核对主题一一对应
- [x] 2.3 在主导航和内容区增加「项目文档」Tab，写入项目简介、Boot 2/3 依赖、JDBC/MyBatis、字段与策略、Digest、LIKE、多数据源、Monitor/Playground 和常见问题章节；通过章节 ID 与目录链接测试验证所有主题可导航
- [x] 2.4 为项目文档配置和代码示例增加只读代码块、复制按钮及共享 `aria-live` 状态节点；通过资源测试验证每个复制按钮都能定位同一示例容器内的代码文本

## 3. 帮助与复制交互

- [x] 3.1 在 `app.js` 实现共用帮助控制器，根据 `data-help-topic` 克隆静态模板并打开弹窗，同时把焦点移至关闭按钮；通过浏览器或 DOM 行为测试验证六个入口展示正确标题与内容
- [x] 3.2 实现关闭按钮、遮罩点击和 Escape 三种关闭路径，并在关闭后恢复触发按钮焦点；通过行为测试逐一验证弹窗关闭且页面输入数据保持不变
- [x] 3.3 使用事件委托实现 `data-copy-target` 示例复制，复用 `utils.copyToClipboard`，成功时更新按钮文字和 `aria-live`，失败时显示错误且保留可选文本；通过模拟成功与失败分支验证反馈
- [x] 3.4 将帮助和文档控制器接入现有初始化流程，并验证首次进入仍激活工作台、选择「项目文档」仅执行前端 Tab 切换且不发起新业务 API 请求

## 4. 样式与响应式

- [x] 4.1 在 `style.css` 增加隔离的 `section-head` 与 `help-` 样式，实现紧凑帮助按钮、遮罩、模态窗口及受限高度正文；通过桌面浏览器检查弹窗不改变当前功能区布局
- [x] 4.2 增加 `docs-` 样式，实现桌面左侧目录、右侧内部滚动正文、代码块及复制按钮布局；通过长文档检查 Monitor 整页高度不随正文持续增长
- [x] 4.3 在 900px 响应式规则中将文档布局切换为单栏和横向滚动目录，并调整模态窗口边距及代码区溢出；通过窄屏浏览器检查无页面级横向滚动

## 5. 集成验证与文档状态

- [x] 5.1 运行 `mvn -pl securt-kit-monitor -am "-Dmaven.test.skip=false" test`，确认 Core 与 Monitor 测试全部通过
- [x] 5.2 在 test-boot2 的 `/monitor/` 完成浏览器冒烟：依次验证六个帮助入口、三种关闭方式、项目文档章节导航、复制成功反馈和现有功能 Tab 可继续使用
- [x] 5.3 在不大于 900px 视口并阻断外部网络的条件下验证帮助和项目文档可阅读、区域内部滚动且无页面级横向溢出
- [x] 5.4 将 `docs/superpowers/specs/2026-09-08-monitor-help-and-project-docs-design.md` 状态更新为已实现，并核对最终 UI 与 OpenSpec specs、design 和验收场景一致
